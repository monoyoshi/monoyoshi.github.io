// scroll to top
function topScroll() {
    document.documentElement.scrollTop = 0;
    document.body.scrollTop = 0; // safari is special apparently
};

// light/dark mode switcher
// var ldButton = document.getElementById("lightdark-mode");